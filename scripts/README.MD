# CheckDot Solana Bridge Scripts

## Build & Deploy

Install dependencies:

```sh
yarn
```

Build dependencies:

```sh
anchor build
```

Run below to deploy the contract:

```sh
anchor deploy --provider.cluster Devnet
```